# 共享数据约定 v1

本文只定义两类引擎共用的字段。业务请求和结果分别由
[决策引擎规范](decision-engine-v1.md)与[读牌引擎规范](opponent-analysis-engine-v1.md)
定义，避免同一业务规则在多份文档中重复维护。

## 1. 座位

绝对座位使用整数 `0..3`：

- `0`：起家东
- `1`：起家南
- `2`：起家西
- `3`：起家北

座位不会随当前庄家或界面视角旋转。协议不得使用“自家、下家、对家、上家”等相对字符串
代替绝对座位；相对方位由宿主显示层换算。

## 2. 牌

规范牌字符串：

- 数牌：`1m` 至 `9m`、`1p` 至 `9p`、`1s` 至 `9s`
- 赤五：`5mr`、`5pr`、`5sr`
- 字牌：`E`、`S`、`W`、`N`、`P`、`F`、`C`
- 暗牌占位符：`?`

`ronWaits` 等按 34 种牌族输出的字段不区分赤五，必须使用普通 `5m`、`5p`、`5s`。
动作中的实体牌可以保留赤五后缀。

## 3. 数值

- 整数必须位于字段规定范围内。
- 浮点数必须有限，不得为 `NaN`、`Infinity` 或 `-Infinity`。
- 概率位于闭区间 `[0, 1]`，允许精确等于 `0` 或 `1`。
- 除字段另有规定外，概率和容差为 `1e-4`。
- JSON 布尔值不视为数值。

接收方可以吸收不超过容差的 float32 边界漂移并钳制到 `[0, 1]`，但必须拒绝实质越界。

## 4. MJAI 历史

`events` 是从当前小局 `start_game` 或 `start_kyoku` 开始，到当前位置为止的规范 MJAI
事件数组。v1 请求始终携带完整历史，不以增量片段替代完整内容。

宿主负责：

- 生成符合请求座位和可见性模式的事件；
- 保持事件顺序和动作字段合法；
- 在公开输入中隐藏不可见手牌；
- 在同一请求中提供与摘要一致的不可变数组。

引擎可以缓存并比较历史前缀。历史增长时可以只处理后缀；回退、分叉或内容变化时必须恢复
共同前缀或完整重放。语言运行时随机化的 `hash()` 不可用作跨进程身份。

## 5. 位置和历史摘要

两类业务请求都包含：

| 字段 | 类型 | 语义 |
| --- | --- | --- |
| `sessionId` | string | 增量状态的隔离键。 |
| `positionId` | string | 当前节点的宿主身份。 |
| `historyDigest` | string | 规范化 `events` 的 SHA-256 摘要。 |

`historyDigest` 的格式为 `sha256:` 加 64 个小写十六进制字符。宿主按以下方式生成摘要：

1. 递归按键名排序 JSON 对象，保持数组顺序；
2. 使用无缩进、无多余空格的 `,` 和 `:` 分隔符；
3. 非 ASCII 字符直接编码为 UTF-8，不转换成 `\u` 转义；
4. 对所得 UTF-8 字节计算 SHA-256。

引擎通常只需原样回显摘要；自行校验时必须使用同一规范化方法。

业务结果必须原样回显这三个字段。`positionId` 可以随牌局树结构变化，`historyDigest` 用于
确认实际历史内容；宿主应用结果时会同时检查二者。

## 6. 引擎指纹

初始化成功后，引擎在 `fingerprint` 字段返回稳定指纹，之后每个业务结果都必须在
`engineFingerprint` 字段回显相同值。
指纹至少覆盖：

```text
engineId
engineVersion
modelSha256
canonicalEffectiveOptions
outputSchema
resultSemanticsVersion
```

文件路径和显示名称不得参与结果身份。权重内容、有效参数或后处理语义改变时，指纹必须改变。

## 7. 优先级和耗时

`priority` 是宿主调度提示：

- `play`：直接影响对局推进；
- `interactive`：用户当前正在查看；
- `background`：自动分析等后台任务。

引擎可以串行处理，但不应该让后台任务永久阻塞更高优先级任务。

业务结果可以包含：

```json
{
  "timing": {
    "preprocessMs": 2.1,
    "queueMs": 0.2,
    "inferenceMs": 67.4,
    "totalMs": 70.3
  }
}
```

`totalMs` 应该提供；其他分量在能准确测量时提供。所有耗时均为非负毫秒数，不包含宿主的
界面动画或人为思考延迟。

## 8. 过时结果

请求执行期间，宿主可能切换节点、分支、引擎或输入模式。宿主只在以下身份仍匹配时应用
结果：

- 当前牌局和 `positionId`
- `historyDigest`
- 当前 `engineFingerprint`
- 业务类型需要的座位、角色和输入模式
- 当前缓存 epoch

不匹配的结果不得覆盖当前界面或写入错误节点。引擎仍应正常结束请求并保持会话可用；是否
真正中断由 `cancellation` 能力决定。
