# 架构与职责边界

## 1. 目标

引擎架构需要同时满足：

- 程序升级不要求重新打包所有权重。
- 权重更新不要求更新界面程序。
- 第三方可以只实现协议，不需要阅读 Vue、Electron 或牌局树代码。
- 一份重模型权重可以服务多个座位和分析用途，避免重复占用内存。
- 引擎崩溃、超时或返回非法数据时，牌局和界面仍由程序后端保护。
- 分析缓存能够准确区分引擎版本、权重内容和有效参数。

## 2. 术语

### 2.1 宿主

“宿主”指管理牌局、引擎进程和用户配置的应用程序。协议的直接对端是宿主后端；
具体界面框架、桌面容器和编程语言不属于本协议。

### 2.2 引擎实现

“引擎实现”是可以独立启动的推理程序。它包含：

- 权重加载与兼容性校验
- 模型结构
- 输入特征编码
- 增量状态管理
- 推理
- 模型内部输出到标准协议结果的转换

决策与对手分析是两类相互独立的引擎。每个引擎必须提供可选择的入口文件和稳定引擎
ID，不能依赖宿主按名称分派模型实现。同一引擎可以声明多种权重格式，并按格式选择
相应的网络结构和输入适配。

### 2.3 模型权重

“模型权重”是 `.pth` 等被动数据文件。权重本身不实现协议、不能报告状态，也不能脱离
兼容引擎运行。

### 2.4 模型配置

“模型配置”（profile）是用户在引擎窗口看到的一行，至少包含：

- 显示名称
- 引擎文件路径
- 引擎 ID
- 权重路径
- 有效参数
- 设备偏好
- 排序位置

同一引擎实现可以拥有多个模型配置。列表的上移和下移只改变显示顺序，不改变任务优先级。

### 2.5 引擎进程与模型实例

“引擎进程”是程序启动的子进程。“模型实例”是该进程内加载的一份神经网络权重。

v1 推荐一个选中的模型配置对应一个长期存活的引擎进程和一份模型实例。不得为了四个
座位默认加载四份相同权重。

### 2.6 会话

“会话”（session）是引擎内部相互隔离的增量输入状态。会话至少由以下维度区分：

- 牌局
- 座位
- 用途

例如：

```text
game-17:seat-0:play
game-17:seat-0:recommendation
game-17:seat-2:play
game-17:seat-2:auto-analysis
```

一个模型实例可以服务任意数量的会话。每个会话必须拥有独立的增量状态、已处理事件数
和历史摘要；模型网络参数可以共享。

### 2.7 任务

“任务”是一次有请求 ID 的推理请求。任务与会话不同：一个会话会先后产生多个任务。

## 3. 程序职责

### 3.1 界面职责

Vue 界面负责：

- 引擎列表和模型配置编辑
- 当前选中项
- 加载、运行、空闲和错误状态的视觉表达
- 推荐条、概率、原始值和耗时的展示
- 用户触发的切换、重载、取消和清理缓存

Vue 界面不得：

- 直接启动或终止引擎进程
- 直接读取 `.pth`
- 直接接收引擎 stdout
- 自行判断模型是否兼容
- 依据模型内部 action index 推导麻将动作
- 把 UI 颜色、CSS 长度或动画参数写入引擎协议

### 3.2 Electron 与引擎管理器职责

Electron 或专用程序后端负责：

- 发现并校验 `engine.json`
- 启动用户实际选择的引擎文件，不依据配置名猜测引擎实现
- 保存模型配置和选择状态
- 使用可执行文件与参数数组启动进程，不拼接 shell 命令
- 建立 JSON-RPC/JSONL 通道
- 请求超时、取消、进程退出和重启
- 汇总引擎和任务状态并发送给界面
- 控制引擎窗口中的添加、删除、排序、复制和恢复默认
- 记录诊断日志，但不把非协议日志混入 stdout

### 3.3 牌局后端职责

牌局后端负责：

- 完整牌局状态、规则和结算
- 生成规范化 MJAI 历史
- 计算合法动作候选
- 给任务分配牌局、节点、座位、用途和优先级
- 判定过时结果并丢弃
- 自动分析、对局预取和交互请求的调度
- 输出结构、数值范围和动作合法性校验
- 实际向听、实际振听/无役和实际铳率的计算
- 缓存键、缓存持久化和旧缓存迁移

程序必须把合法动作作为候选列表传给 `decision` 引擎。引擎返回的候选 ID 必须再次由
程序校验，模型不能越过牌局规则执行动作。

对手分析结果的规则语义属于引擎。例如，一个引擎可以在输出前强制执行：

- 立直后通过的牌铳率为 0
- 同巡振听范围内的牌铳率为 0
- 对手自己打过的牌铳率为 0
- 立直后的听牌概率为 100%，但不覆盖振听/无役概率

第三方引擎可以采用不同模型或不同后处理方法，但必须返回可直接展示的最终概率。
程序只校验字段完整性和数值范围，不对预测结果再次 mask。

## 4. 引擎职责

所有引擎必须：

- 实现 `engine.hello` 和 `engine.initialize`
- 验证引擎类型、权重格式和输入 Schema
- 报告实际加载的引擎版本、权重摘要和有效参数
- 不在协议 stdout 输出日志
- 为每个请求返回结果或标准错误
- 支持多个不同 `sessionId`
- 正确处理同一会话历史增长、回退和分支分叉
- 将内部张量和 action index 转换成协议数据
- 应返回总耗时，并在能够准确测量时返回预处理、排队和推理分量

引擎不负责：

- 牌局树和存档
- 用户当前停留节点
- 哪个缓存应该写入存档
- UI 排版
- 牌局推进和动作执行
- 对局动画和 AI 思考延迟

### 4.1 `decision` 引擎

`decision` 引擎负责：

- 从一个座位的公开/私有 MJAI 视角构建模型输入
- 维护每个会话的增量状态
- 对程序提供的合法候选评分
- 返回最佳候选、标准化概率和原始值
- 声明参数 Schema、默认值，并按有效参数计算最终概率

决策引擎的模型网络通常是无会话状态的共享计算资源。增量状态应位于会话层，不得存放
在共享模型实例中。

### 4.2 `opponent-analysis` 引擎

`opponent-analysis` 引擎负责：

- 声明当前模型支持的手牌可见性输入模式
- 按程序指定的输入模式构建预测输入
- 同时或分别预测另外三个座位
- 返回已经完成引擎自身规则修饰、可直接展示的最终概率
- 返回向听分布、振听/无役概率和 34 种牌的铳率

所有对手分析引擎必须支持只含公开信息的 `public` 模式，也可以声明支持
`full-information` 模式。程序后端在初始化后读取当前模型的有效能力：

- 未开启“显示手牌”时始终使用 `public`。
- 开启“显示手牌”、当前牌谱确有完整手牌且模型支持时，使用 `full-information`。
- 缺少完整手牌或模型不支持时，回退 `public`，不得伪造明牌输入。

实际值由程序后端使用完整牌局信息另行计算。切换输入模式必须切换或重置增量会话。

## 5. 进程与调度模型

### 5.1 决策引擎

默认运行模型：

```text
一个决策引擎进程
  一个共享模型实例
    多个 play 会话
    多个 recommendation 会话
    多个 auto-analysis 会话
```

决策任务可以在进程内部串行，也可以在能力允许时批处理。v1 不要求并行执行。
程序必须保证交互和对局推进任务高于后台自动分析。

### 5.2 对手分析

对手分析使用独立进程和独立任务队列。它不得阻塞决策任务或对局动画。若后台预测
跟不上对局，程序可以取消或跳过已经过时的节点。

### 5.3 状态指示

界面默认显示两个模型指示灯：

- 决策引擎
- 对手分析

引擎只报告语义状态，程序映射为颜色：

| 语义状态 | 默认显示 |
| --- | --- |
| `starting` / `loading` / `reloading` | 橙色 |
| `ready` | 灰色 |
| 任一任务为 `running` | 绿色 |
| `stopping` / `stopped` | 灰色 |
| `error` | 红色 |

决策引擎的任务通知携带座位和用途，悬停文字可以显示“正在分析：下家出牌”，但不需要为
四个视角分别显示圆点。

状态优先级为：

```text
error > starting/loading/reloading > running task > ready
```

程序可以根据请求生命周期合成任务状态，但引擎应该发送实际 `queued` 和 `running`
通知，以区分请求已经发出与模型真正开始推理。

## 6. 缓存与身份

### 6.1 引擎指纹

初始化成功后，引擎必须返回稳定指纹。推荐计算：

```text
SHA-256(
  engineId
  + engineVersion
  + modelSha256
  + canonicalEffectiveOptions
  + outputSchemaVersion
)
```

程序不得只使用文件路径识别模型。同一路径的权重内容发生变化时，指纹必须变化。

### 6.2 分析缓存键

程序缓存至少包含：

```text
positionDigest
seat
role
inputMode
engineFingerprint
```

对手分析还必须包含受控座位和输出 Schema。`public` 与 `full-information` 结果不得
互用。切换引擎不需要删除旧缓存；程序只读取与当前指纹和输入模式匹配的结果。

`engineFingerprint` 标识引擎、权重、有效参数和引擎内部后处理语义共同产生的结果。
宿主不再为预测结果追加独立的规则修饰版本。

牌谱使用顶层 `analysisSources` 表保存完整 `cacheFingerprint`、显示名称和输出 Schema。
节点缓存键只保存来源短引用、座位及分析阶段，不在每个节点重复完整 SHA-256。显示名称
不参与身份计算；模型改名或移动文件不会使缓存失效。只有模型内容、引擎版本、有效参数、
输入模式或引擎内部结果语义变化时，才生成新的来源身份。

当前来源没有精确缓存时，程序可以临时显示输出 Schema 兼容的旧来源结果，但必须将其
标记为旧缓存并立即调度当前来源。旧结果只在新结果成功写入后移除；自动分析进度只承认
当前来源的精确缓存。

旧存档中没有引擎指纹的缓存视为 `legacy-unknown`，不得静默当作当前模型结果使用。

## 7. 故障隔离

引擎错误不得破坏牌局后端。程序必须处理：

- 清单无效
- 协议主版本不兼容
- 权重不存在或摘要不匹配
- 初始化超时
- 推理超时
- 非法 JSON
- stdout 非协议内容
- 结果引用未知候选
- 概率为 NaN、Infinity 或超出范围
- 引擎主动报错
- 子进程异常退出

引擎进程退出时，程序把对应引擎标记为错误并取消其所有未完成任务。是否自动重启由程序
策略决定，v1 建议最多自动重试一次，避免崩溃循环。
