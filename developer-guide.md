# 第三方引擎开发指南

状态：公开草案 1.0

本指南面向希望为本程序开发决策引擎或对手分析引擎的开发者。

## 1. 最小实现流程

1. 实现一个从标准输入逐行读取 JSON-RPC 2.0 消息的长期运行进程。
2. 在标准输出中只写一行一个 JSON 消息；日志写入标准错误。
3. 实现 `engine.hello`、`engine.initialize`、`engine.getStatus`、`engine.shutdown`。
4. 根据引擎类型实现 `decision.analyze` 或 `opponent.predict`。
5. 提供符合 [package-format-v1.md](package-format-v1.md) 的 `engine.json`。
6. 使用多个 `sessionId` 测试独立的增量状态。
7. 通过程序提供的协议一致性测试后再分发。

公共协议见 [protocol-v1.md](protocol-v1.md) 和
[data-contracts-v1.md](data-contracts-v1.md)。业务实现分别见
[decision-engine-v1.md](decision-engine-v1.md) 与
[opponent-analysis-engine-v1.md](opponent-analysis-engine-v1.md)。

## 2. 最小进程循环

以下伪代码只说明消息边界，不限定实现语言：

```text
load engine manifest
for each UTF-8 line from stdin:
    parse one JSON object
    validate JSON-RPC envelope
    dispatch method
    write exactly one response or the required notifications
    flush stdout
```

不得把进度文字、模型日志或堆栈信息写到标准输出，否则会破坏协议流。错误详情可以写标准错误，并通过结构化 JSON-RPC 错误返回简短摘要。

## 3. 会话与增量计算

`sessionId` 代表一条独立、可回退或可重建的分析上下文。引擎不得假设请求永远按牌谱时间顺序到达。

建议每个会话保存：

- 已确认的事件前缀摘要。
- 可复用的编码器状态或模型隐状态。
- 当前视角、规则和角色。
- 最近一次完成请求的序号。

收到新请求时可以比较 `historyDigest` 和事件前缀：

- 新历史是原历史的后缀扩展时，做增量更新。
- 历史回退、切换分支或摘要不一致时，从共同前缀恢复或重建。
- `session.reset` 后丢弃该会话的全部增量状态。
- 一个会话取消不得污染其他会话。

共享一份模型权重不等于共享牌局状态。四个视角、自动分析和界面即时分析必须使用不同会话。

## 4. 决策引擎要求

完整规范见 [决策引擎规范 v1](decision-engine-v1.md)。以下仅为开发检查摘要：

- 只在程序传入的合法候选动作中评分。
- 用稳定的 `candidateId` 回传结果，不自行重写动作 JSON。
- 对所有候选返回有限数值；不得返回 `NaN` 或无穷大。
- `probability` 表示归一化后的选择概率，`rawValue` 表示引擎原始输出。
- 引擎可以选出最佳动作，但程序仍负责执行规则校验和实际推进。
- 强制摸切、无合法选择等情况由程序判断，不应为了点亮状态灯而调用模型。

## 5. 对手分析引擎要求

完整规范见 [读牌引擎规范 v1](opponent-analysis-engine-v1.md)。以下仅为开发检查摘要：

- 必须支持 `public` 输入模式，可以选择支持 `full-information`。
- 静态清单、hello 和初始化结果都应准确声明支持模式，当前权重的初始化结果最权威。
- 只能在 `full-information` 请求中使用完整手牌；`public` 请求中的暗牌必须保持未知。
- 返回引擎认可的最终预测，不混入柱高、颜色、百分比字符串等界面显示值。
- 引擎必须自行完成其结果需要的规则修饰；程序不会统一追加现物、同巡振听或立直后听牌率 mask。
- “实际值”由程序根据完整牌谱或牌山计算，不属于预测引擎输出。
- 概率应保留足够精度，显示时的有效数字格式化由界面负责。

## 6. 状态与错误

引擎必须在耗时阶段及时发送语义状态：

- `starting`：进程已启动但尚未完成握手，由程序进程管理器合成。
- `loading`：首次载入或重新载入模型。
- `ready`：模型已就绪且没有运行任务。
- `error`：无法继续使用，例如权重不兼容或加载失败。

推理过程通过 `task.status=queued/running/completed` 报告。状态不得由界面根据“请求似乎
很慢”来猜测。任务状态应携带 `requestId`、`sessionId`、引擎角色和座位，使程序能够
把一个共享决策引擎映射到正确的业务位置。

错误响应应说明：

- 稳定错误代码。
- 适合显示给用户的简短消息。
- 可选的结构化详情，例如不兼容的模型格式。

不得把本机绝对路径、环境变量、访问令牌或完整输入数据放进面向用户的错误文本。

## 7. 取消和过期结果

程序可能发送 `request.cancel`，但取消是协作式的。引擎应尽快停止尚未开始或可中断的任务。

无论引擎是否支持真正中断，程序都会依据 `requestId`、`sessionId` 和 `historyDigest` 丢弃过期结果。引擎不得因为旧请求晚返回而覆盖较新的会话状态。

## 8. 兼容性规则

- 协议主版本不同视为不兼容。
- 程序可以接受不高于自身支持范围的协议次版本。
- 新增可选字段只提升次版本；删除字段或改变语义提升主版本。
- 接收方应忽略未知的可选字段。
- 必填字段缺失、枚举值未知或数值非法时必须明确报错。
- 可选能力通过 `capabilities` 协商，不得通过引擎名称猜测。

引擎私有参数放在经运行时 `engine.hello.optionsSchema` 声明的 `options` 中。程序动态生成
设置项，不得硬编码温度等只属于某些引擎的参数。

## 9. 安全要求

第三方引擎与普通本地可执行程序具有相同风险。程序侧应：

- 安装或首次启用时提示来源与签名状态。
- 直接传递参数启动进程，不经过 Shell。
- 默认只传协议所需数据，不传环境中的秘密信息。
- 限制单行消息长度、未完成请求数和标准错误缓存大小。
- 对超时、崩溃和输出洪泛做隔离，允许单独重启引擎。
- 不将引擎返回的字符串当作 HTML 渲染。

v1 不承诺操作系统级沙箱。用户只应运行可信来源的引擎。

## 10. 一致性测试

一个可发布引擎至少应通过：

- 正常握手、初始化、状态查询和关闭。
- 模型文件不存在、摘要不符、格式不兼容。
- 两个以上会话交错请求且状态互不污染。
- 历史向前推进、回退、切换分支和重置。
- 请求取消以及取消后继续使用。
- 非法动作 ID、非法概率、`NaN` 和缺失字段。
- 推理过程中崩溃，程序可显示错误并重新加载。
- 大量标准错误日志不会阻塞协议输出。
- 决策引擎四视角共享模型时，仍保持独立增量状态。
- 对手分析引擎在没有隐藏手牌时可完成预测。
- 支持明牌的对手分析引擎能在两种模式间切换，且缓存和增量状态不串用。

后续应提供一个轻量参考引擎和命令行测试器，第三方开发者无需启动完整 Electron 界面即可验证协议。

## 11. 发布检查表

- 引擎和模型具有稳定且不冲突的 ID。
- `engine.json` 与 `model.json` 通过对应 JSON Schema。
- Windows 入口可以在无开发环境的干净机器启动。
- 所有依赖和动态库随包提供或有明确安装说明。
- LICENSE、NOTICE、模型来源和再分发权限清晰。
- 默认参数不会修改用户文件或访问网络。
- 协议版本和变更日志已记录。
